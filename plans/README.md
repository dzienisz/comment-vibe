# Implementation Plans

Generated by the improve skill on 2026-07-11 against commit `3249b82`. Execute in the order below. Each executor must read its plan fully, honor all STOP conditions, run every verification gate, and update only its status row when finished.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Establish an automated characterization-test baseline | P1 | M | — | DONE |
| 002 | Prevent stale analyses from rendering | P1 | S | 001 | DONE |
| 003 | Make model-result rendering type-safe and injection-safe | P1 | S | 001 | DONE |
| 004 | Make DOM discovery and cleanup incremental and leak-free | P1 | M | 001, 002 | TODO |
| 005 | Make modern and legacy AI session lifecycle bounded and race-free | P1 | M | 001, 002 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- Plan 001 lands first because every later change touches central logic in `content.js`, and the repository currently has no automated regression suite.
- Plans 002 and 003 are independent after Plan 001 and may be executed in either order.
- Plan 004 depends on Plan 002 because input cleanup must use the same request invalidation semantics rather than introducing a second cancellation path.
- Plan 005 depends on Plan 002 because per-analysis cancellation must compose with session creation, cloning, prompting, and destruction.
- Execute each plan in its own branch/worktree and review it before starting a dependent plan. Rebase or refresh later plans after predecessors land.

## Repository-wide constraints

- Ship raw Chrome extension files: no npm, package manager, bundler, transpiler, or third-party dependency.
- Preserve both `LanguageModel` and `window.ai.languageModel` API paths.
- Preserve intentional CSS `!important` rules.
- Do not add or delete comments or documentation in runtime source unless the operator explicitly requests it.
- Keep user comment text out of logs and error messages.

## Findings considered and rejected

- Removing CSS `!important`: rejected because it intentionally protects extension UI from host-page styles.
- Removing the legacy `window.ai.languageModel` path: rejected because Chrome 127–137 support is an explicit compatibility requirement.
- Replacing `<all_urls>` immediately: rejected because passive support across arbitrary comment sites is current product behavior; permission narrowing should be a separate product decision.
- Treating best-effort translation fallback as an error: rejected because fallback to English is explicitly documented behavior.
- Adding a framework or DOM-test dependency: rejected because the repository intentionally ships and develops without dependencies.
